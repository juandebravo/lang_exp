<!DOCTYPE html>
<html>
<head>
  <title>LangExp</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :all %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <div id="title">
        <h1><a href="/">Language Experiment</a></h1>
      </div>
  		<% if user_signed_in? %>
  			<div class="signed_in_box">
        	<%= link_to "Sign out", destroy_user_session_path,  :method=>'delete' %>
        </div>
        <div class="signed_in_box">|</div>
        <div class='signed_in_box'>
        	Welcome  <%= link_to current_user.username, user_path(current_user.username) %>
        </div>
        <div id="search" class="signed_in_box">
          <input id="search_text" type="text"/>
          <input type="button" value="search" id="search_button"/>
        </div>
  		<% end %>
    </div>
    <div id="content">
      <% if !notice.nil? %>
				<div class="notice"><%= notice %></div>
			<% end %>
      <%= yield %>
    </div>
    <div id="footer">
      Ruby on Rails power!!
    </div>
  </div>
</body>
<%= javascript_tag do -%>
  $('#search_button').click(function() {
    location.href = '/users/' + $('#search_text').val();
  });
<% end -%>
</html>
